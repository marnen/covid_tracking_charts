language: ruby
cache: bundler

before_install:
  - gem install bundler
  - nvm install

before_script:
  - RAILS_ENV=test bundle exec rails webpacker:compile

script:
  - bundle exec rspec
  - bundle exec cucumber
