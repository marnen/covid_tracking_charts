= form_with url: choose_states_path do
  = label_tag :state, 'State'
  = select_tag :state, options_from_collection_for_select(states.sort_by(&:name), 'abbr', 'name', @state.abbr)
  = submit_tag 'Go'
